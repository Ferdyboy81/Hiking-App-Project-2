

<%= form_for ([@trails, @trails.reviews.build]) do |f| %>
  <%= f.text_field :text %><br>
  Average Rating: <%= rating_for @trails, 'rating' %>
  <%= f.submit 'Submit', :class => 'btn btn-default' %>
<% end %>
