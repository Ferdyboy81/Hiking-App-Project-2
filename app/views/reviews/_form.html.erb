

<%= form_for ([@trails, @trails.reviews.build]) do |f| %>
  <%= f.text_field :text %><br>
   Your rating:<%= rating_for @trails, 'rating',  disable_after_rate: false  %>
  <%= f.submit 'Submit', :class => 'btn btn-default' %>
<% end %>
